apply plugin: "war"

bootWar {
    mainClass = "com.dazzle.asklepios.GatewayApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
