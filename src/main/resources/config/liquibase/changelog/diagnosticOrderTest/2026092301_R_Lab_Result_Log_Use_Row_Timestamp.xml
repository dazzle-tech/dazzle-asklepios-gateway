<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
    http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

  <changeSet id="2026092301" author="hanan">
    <sql splitStatements="false" endDelimiter=";">
      <![CDATA[
      CREATE OR REPLACE FUNCTION public.trg_lab_result_log_fn()
      RETURNS trigger AS $$
      BEGIN
        IF TG_OP = 'INSERT' THEN
          IF NEW.result_value_number IS NOT NULL
             OR (NEW.result_value_text IS NOT NULL AND btrim(NEW.result_value_text) <> '') THEN
            INSERT INTO public.lab_result_log (
              result_id, result_date, result_by, result_value
            )
            VALUES (
              NEW.id,
              COALESCE(NEW.last_modified_date, NEW.created_date),
              COALESCE(NEW.last_modified_by, NEW.created_by),
              COALESCE(NEW.result_value_number::text, btrim(NEW.result_value_text))
            );
          END IF;

          RETURN NEW;

        ELSIF TG_OP = 'UPDATE' THEN
          IF (OLD.result_value_number IS DISTINCT FROM NEW.result_value_number)
             OR (OLD.result_value_text IS DISTINCT FROM NEW.result_value_text) THEN

            IF NEW.result_value_number IS NOT NULL
               OR (NEW.result_value_text IS NOT NULL AND btrim(NEW.result_value_text) <> '') THEN
              INSERT INTO public.lab_result_log (
                result_id, result_date, result_by, result_value
              )
              VALUES (
                NEW.id,
                COALESCE(NEW.last_modified_date, NEW.created_date),
                COALESCE(NEW.last_modified_by, NEW.created_by),
                COALESCE(NEW.result_value_number::text, btrim(NEW.result_value_text))
              );
            END IF;

          END IF;

          RETURN NEW;
        END IF;

        RETURN NEW;
      END;
      $$ LANGUAGE plpgsql;
      ]]>
    </sql>

    <rollback>
      <sql splitStatements="false" endDelimiter=";">
        <![CDATA[
        CREATE OR REPLACE FUNCTION public.trg_lab_result_log_fn()
        RETURNS trigger AS $$
        BEGIN
          IF TG_OP = 'INSERT' THEN
            IF NEW.result_value_number IS NOT NULL
               OR (NEW.result_value_text IS NOT NULL AND btrim(NEW.result_value_text) <> '') THEN
              INSERT INTO public.lab_result_log (
                result_id, result_date, result_by, result_value
              )
              VALUES (
                NEW.id,
                now(),
                COALESCE(NEW.last_modified_by, NEW.created_by),
                COALESCE(NEW.result_value_number::text, btrim(NEW.result_value_text))
              );
            END IF;

            RETURN NEW;

          ELSIF TG_OP = 'UPDATE' THEN
            IF (OLD.result_value_number IS DISTINCT FROM NEW.result_value_number)
               OR (OLD.result_value_text IS DISTINCT FROM NEW.result_value_text) THEN

              IF NEW.result_value_number IS NOT NULL
                 OR (NEW.result_value_text IS NOT NULL AND btrim(NEW.result_value_text) <> '') THEN
                INSERT INTO public.lab_result_log (
                  result_id, result_date, result_by, result_value
                )
                VALUES (
                  NEW.id,
                  now(),
                  COALESCE(NEW.last_modified_by, NEW.created_by),
                  COALESCE(NEW.result_value_number::text, btrim(NEW.result_value_text))
                );
              END IF;

            END IF;

            RETURN NEW;
          END IF;

          RETURN NEW;
        END;
        $$ LANGUAGE plpgsql;
        ]]>
      </sql>
    </rollback>
  </changeSet>

</databaseChangeLog>

