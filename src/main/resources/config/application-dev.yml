
logging:
  level:
    ROOT: DEBUG
    org.hibernate.SQL: DEBUG
    com.dazzle.asklepios: DEBUG

management:
  zipkin: # Use the "zipkin" Maven profile to have the Spring Cloud Zipkin dependencies
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0 # report 100% of traces

services:
  asklepios-general-service-url: http://localhost:9090
  asklepios-setup-service-url :  http://localhost:8082
spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false # we use Webpack dev server + BrowserSync for livereload
  jackson:
    serialization:
      indent-output: true
  liquibase:
    # Remove 'faker' if you do not want the sample data to be loaded automatically
    contexts: dev, faker
    url: jdbc:postgresql://localhost:5433/ask
  mail:
    host: localhost
    port: 25
    username:
    password:
  messages:
    cache-duration: PT1S # 1 second, see the ISO 8601 standard
  r2dbc:
    url: jdbc:postgresql://localhost:5433/ask
    #    url: r2dbc:postgresql://host.docker.internal:5432/asklepios
    username: postgres
    password: 147741
  thymeleaf:
    cache: false
  cloud:
    gateway:
      default-filters:
        - JWTRelay
      routes:
        - id: reference-data
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/reference-data/**
          filters:
            - StripPrefix=0

        - id: appointment
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/appointment/**
          filters:
            - StripPrefix=0

        - id: attachment
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/attachment/**
          filters:
            - StripPrefix=0

        - id: dental
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/dental/**
          filters:
            - StripPrefix=0

        - id: dvm
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/dvm/**
          filters:
            - StripPrefix=0

        - id: encounter
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/encounter/**
          filters:
            - StripPrefix=0

        - id: general
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/general/**
          filters:
            - StripPrefix=0

        - id: lab
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/lab/**
          filters:
            - StripPrefix=0

        - id: medications
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/medications/**
          filters:
            - StripPrefix=0

        - id: observation
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/observation/**
          filters:
            - StripPrefix=0

        - id: pas
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/pas/**
          filters:
            - StripPrefix=0

        - id: rad
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/rad/**
          filters:
            - StripPrefix=0

        - id: setup
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/setup/**
          filters:
            - StripPrefix=0

        - id: newsetup
          uri: ${services.asklepios-setup-service-url}
          predicates:
            - Path=/api/setup/**
          filters:
            - StripPrefix=0

        - id: auth
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/auth/**
          filters:
            - StripPrefix=0
        - id: inventory
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/transaction/**
          filters:
            - StripPrefix=0

        - id: operation
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/operation/**
          filters:
            - StripPrefix=0

        - id: procedures
          uri: ${services.asklepios-general-service-url}
          predicates:
            - Path=/procedures/**
          filters:
            - StripPrefix=0

        - id: setup-docs
          uri: ${services.asklepios-setup-service-url}
          predicates:
            - Path=/setup-service/v3/api-docs
          filters:
            - StripPrefix=1

      httpclient:
        pool:
          max-connections: 1000

server:
  port: 8080
  # make sure requests the proxy uri instead of the server one
  forward-headers-strategy: native

asklepios:
  cors:
    allowed-origins: 'http://localhost:4200,https://localhost:4200,https://localhost:3100,http://localhost:3100'
    allowed-origin-patterns: 'https://*.githubpreview.dev'
    allowed-methods: '*'
    allowed-headers: '*'
    exposed-headers: 'Authorization,Link,X-Total-Count,X-${asklepios.clientApp.name}-alert,X-${asklepios.clientApp.name}-error,X-${asklepios.clientApp.name}-params'
    allow-credentials: true
    max-age: 1800
  security:
    authentication:
      jwt:
        # This token must be encoded using Base64 and be at least 256 bits long (you can type `openssl rand -base64 64` on your command line to generate a 512 bits one)
        base64-secret: Y2RiNDRmMGM3ODZkM2FhMTEwYWYxZGE0NjQ3ODM5YThkYWFjZWYzMzY2ZDVkMTVhNGEyNDNmZTM0MzQ5NTRhMmVlMmFkNGVlODBmYTkyZWE4ZWYyMDRhZDJiZGU4YzJhMGEyN2U5NGUzMmNmNWU4Yzk2MDVkNzhmY2M3YTgyZDM=
        # Token is valid 24 hours
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
  mail:
    base-url: http://127.0.0.1:8080
    username: systemask73@gmail.com
    password: ocdh hvae hhsz cyxc
